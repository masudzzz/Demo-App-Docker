sudo apt install docker.io -y
sudo systemctl start docker
sudo systemctl enable docker


Create app.py
Create requirements.txt
Create Dockerfile


# Build docker image with all the files and dependencies.
docker built -t myapp-image .

# run docker image
docker run -d -p 5000:5000 myapp-image

# After that go to browser and type -> http://localhost:5000
# Output ->  Hello Docker!
